/* we_ai_agent.c - the agent-mode tool harness (read_file, list_dir, grep,
 * write_file, run_command) with human approval gating for every mutation.
 * Compiled only under --enable-ai (WPE_AI). */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#ifdef WPE_AI
#include "we_ai.h"

/* Agent harness implementation added in the Agent phase. */

#endif /* WPE_AI */

typedef int wpe_ai_agent_translation_unit;
